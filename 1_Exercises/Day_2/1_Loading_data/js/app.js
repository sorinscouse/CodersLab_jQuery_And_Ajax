$( function() {

    var movieList = $( '.list' );
    var movieUrl = 'https://swapi.co/api/films/';

    function insertContent( movies ) {
        //iterate through movies in a loop here
        //generate another li and append it to the movieList
    }

    function loadMovies() {
        //Ajax connection here
    }

    loadMovies();
});
