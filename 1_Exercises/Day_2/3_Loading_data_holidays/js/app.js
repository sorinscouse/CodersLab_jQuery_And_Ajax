$(function() {

    var ul = $("#holidayDates");
    var holidayUrl = '';

    function instertHolidays(days) {
        //Use a method (not keys) to get values from the object, e.g. the for in loop
    }

    function loadHolidays() {
        //Ajax connection here
    }
    loadHolidays();

});
